# Contributing

- Keep marketplace logic in adapters (`src/adapters/*`)
- Keep matching logic in `src/core/match.ts`
- Avoid adding scraping; prefer official APIs
- Document any behavior change in `docs/`
